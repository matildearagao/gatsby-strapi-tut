import React from 'react';
import PropTypes from 'prop-types';

const example = props => {
    return (
        <div>
            
        </div>
    );
};

example.propTypes = {
    
}; 

export default example;